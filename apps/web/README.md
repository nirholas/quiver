# @quiverdex/web

Landing page (`index.html`) and swap app (`app.html`). Static. The app reads the API URL from `?api=` or defaults to `http://localhost:4700` on localhost and `<origin>/api` elsewhere.
